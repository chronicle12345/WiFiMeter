<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="{DynamicResource WindowTitle}" Width="1240" Height="760" MinWidth="920" MinHeight="650" WindowStartupLocation="CenterScreen" Background="#F5F7FB" FontFamily="Segoe UI, Microsoft YaHei UI" FontSize="13" Foreground="#202B43" UseLayoutRounding="True" SnapsToDevicePixels="True">
  <Window.Resources>
    <SolidColorBrush x:Key="Muted" Color="#7C879D" />
    <SolidColorBrush x:Key="Accent" Color="#5A63E8" />
    <Style TargetType="Button">
      <Setter Property="Height" Value="36" />
      <Setter Property="Padding" Value="15,0" />
      <Setter Property="Background" Value="White" />
      <Setter Property="Foreground" Value="#526079" />
      <Setter Property="BorderBrush" Value="#E3E7EF" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Chrome" CornerRadius="8" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.8" />
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.65" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.45" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Background" Value="{StaticResource Accent}" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderThickness" Value="0" />
    </Style>
    <Style x:Key="Segment" TargetType="RadioButton">
      <Setter Property="Height" Value="32" />
      <Setter Property="Padding" Value="15,0" />
      <Setter Property="Foreground" Value="#7C879D" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="RadioButton">
            <Border x:Name="Chrome" CornerRadius="6" Background="Transparent" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Chrome" Property="Background" Value="White" />
                <Setter Property="Foreground" Value="#515CE2" />
                <Setter Property="FontWeight" Value="SemiBold" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Chrome" Property="Background" Value="#E9EDF8" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="Card" TargetType="Border">
      <Setter Property="Background" Value="White" />
      <Setter Property="CornerRadius" Value="12" />
      <Setter Property="BorderBrush" Value="#E9EDF4" />
      <Setter Property="BorderThickness" Value="1" />
    </Style>
    <Style TargetType="DatePicker">
      <Setter Property="Width" Value="136" />
      <Setter Property="Height" Value="31" />
      <Setter Property="BorderBrush" Value="#E3E7EF" />
      <Setter Property="Background" Value="White" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="SelectedDateFormat" Value="Short" />
    </Style>
    <Style x:Key="StartupSwitch" TargetType="CheckBox">
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="CheckBox">
            <Border x:Name="Track" Width="36" Height="20" Background="#D7DDE8" CornerRadius="10">
              <Ellipse x:Name="Thumb" Width="14" Height="14" Margin="3" Fill="White" HorizontalAlignment="Left" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Track" Property="Background" Value="#5A63E8" />
                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Track" Property="Opacity" Value="0.45" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="DataGridColumnHeader">
      <Setter Property="Background" Value="#F8F9FC" />
      <Setter Property="Foreground" Value="#7C879D" />
      <Setter Property="FontWeight" Value="Normal" />
      <Setter Property="Padding" Value="16,12" />
      <Setter Property="BorderThickness" Value="0" />
    </Style>
    <Style TargetType="DataGridCell">
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="16,10" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="DataGridCell">
            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter VerticalAlignment="Center" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style TargetType="DataGridRow">
      <Setter Property="MinHeight" Value="46" />
      <Setter Property="BorderThickness" Value="0" />
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Background" Value="#EEF0FF" />
          <Setter Property="Foreground" Value="#202B43" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="NumericCell" TargetType="TextBlock">
      <Setter Property="HorizontalAlignment" Value="Right" />
      <Setter Property="FontFamily" Value="Segoe UI" />
    </Style>
  </Window.Resources>
  <Grid Background="#F5F7FB">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="208" />
      <ColumnDefinition Width="*" />
    </Grid.ColumnDefinitions>
    <Border Background="White" BorderBrush="#E7EBF3" BorderThickness="0,0,1,0">
      <Grid Margin="22,28,22,22">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
          <StackPanel Orientation="Horizontal">
            <Border Width="34" Height="34" CornerRadius="10" Background="#5A63E8" Margin="0,0,9,0">
              <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="White" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock Text="WiFiMeter" FontFamily="Segoe UI" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center" />
          </StackPanel>
          <TextBlock Text="{DynamicResource Tagline}" Foreground="{StaticResource Muted}" FontSize="11" Margin="1,12,0,0" />
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,34,0,0">
          <TextBlock Text="{DynamicResource Workspace}" Foreground="#A0A9BA" FontSize="10" Margin="10,0,0,12" />
          <Border Background="#EEEFFE" CornerRadius="8" Padding="12,12">
            <StackPanel Orientation="Horizontal">
              <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="#5A63E8" Margin="0,1,12,0" />
              <TextBlock Text="{DynamicResource Overview}" Foreground="#535DE0" FontWeight="SemiBold" />
            </StackPanel>
          </Border>
          <Button x:Name="FolderButton" Content="{DynamicResource OpenData}" BorderThickness="0" Margin="0,8,0,0" AutomationProperties.Name="{DynamicResource OpenDataAccessible}" />
          <Button x:Name="SettingsButton" Content="{DynamicResource Settings}" BorderThickness="0" Margin="0,4,0,0" />
          <Border Background="#F8F9FD" CornerRadius="10" Padding="13" Margin="0,28,0,0">
            <StackPanel>
              <TextBlock Text="{DynamicResource CurrentConnection}" Foreground="{StaticResource Muted}" FontSize="11" />
              <TextBlock x:Name="ConnectionName" Text="{DynamicResource WaitingConnection}" Margin="0,8,0,10" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition />
                  <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <StackPanel>
                  <TextBlock Text="{DynamicResource DownloadSpeed}" Foreground="#7C879D" FontSize="10" />
                  <TextBlock x:Name="DownloadSpeed" Text="0.00 MB/s" Foreground="#5A63E8" FontSize="12" Margin="0,6,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="1">
                  <TextBlock Text="{DynamicResource UploadSpeed}" Foreground="#7C879D" FontSize="10" />
                  <TextBlock x:Name="UploadSpeed" Text="0.00 MB/s" Foreground="#20A7A0" FontSize="12" Margin="0,6,0,0" />
                </StackPanel>
              </Grid>
            </StackPanel>
          </Border>
        </StackPanel>
        <StackPanel Grid.Row="3">
          <Border BorderBrush="#EDF0F5" BorderThickness="0,1,0,0" Padding="0,18,0,0">
            <StackPanel>
              <Grid>
                <TextBlock Text="{DynamicResource AutoStart}" FontSize="12" />
                <CheckBox x:Name="AutoStart" Style="{StaticResource StartupSwitch}" HorizontalAlignment="Right" AutomationProperties.Name="{DynamicResource AutoStart}" />
              </Grid>
              <TextBlock x:Name="StartupDetail" Text="{DynamicResource AutoStartHint}" Foreground="{StaticResource Muted}" FontSize="10" Margin="0,9,0,0" TextWrapping="Wrap" />
              <Button x:Name="StartupSettingsButton" Content="{DynamicResource StartupSettings}" FontSize="10" Padding="6,0" Margin="0,10,0,0" Visibility="Collapsed" />
            </StackPanel>
          </Border>
          <TextBlock Text="{DynamicResource Language}" Foreground="#A0A9BA" FontSize="10" Margin="0,18,0,8" />
          <Border Background="#F0F2F8" CornerRadius="7" Padding="2">
            <UniformGrid Columns="2">
              <RadioButton x:Name="LanguageEnglish" GroupName="Language" Style="{StaticResource Segment}" Content="English" Height="28" Padding="6,0" FontSize="11" AutomationProperties.Name="English" />
              <RadioButton x:Name="LanguageChinese" GroupName="Language" Style="{StaticResource Segment}" Content="简体中文" Height="28" Padding="6,0" FontSize="11" AutomationProperties.Name="简体中文" />
            </UniformGrid>
          </Border>
          <Button x:Name="StopAndExitButton" Content="{DynamicResource StopExit}" Margin="0,14,0,0" FontSize="12" />
          <StackPanel Orientation="Horizontal" Margin="2,19,0,0">
            <TextBlock FontFamily="Segoe MDL2 Assets" Text="" Foreground="#9AA5B8" FontSize="11" Margin="0,2,6,0" />
            <TextBlock Text="{DynamicResource LocalOnly}" Foreground="#9AA5B8" FontSize="10" />
          </StackPanel>
        </StackPanel>
      </Grid>
    </Border>
    <Grid Grid.Column="1" Margin="28,24,28,16">
      <Grid.RowDefinitions>
        <RowDefinition Height="76" />
        <RowDefinition Height="112" />
        <RowDefinition Height="58" />
        <RowDefinition Height="*" />
        <RowDefinition Height="40" />
      </Grid.RowDefinitions>
      <Grid>
        <StackPanel>
          <StackPanel Orientation="Horizontal">
            <TextBlock Text="{DynamicResource Overview}" FontSize="26" FontWeight="SemiBold" />
            <Border x:Name="StatusPill" Background="#E9F6F0" CornerRadius="10" VerticalAlignment="Center" Margin="14,3,0,0" Padding="9,4">
              <TextBlock x:Name="StatusBadge" Text="{DynamicResource WaitingStart}" Foreground="#26996E" FontSize="10" />
            </Border>
          </StackPanel>
          <TextBlock x:Name="HeaderSubtitle" Text="{DynamicResource Subtitle}" Foreground="{StaticResource Muted}" Margin="1,8,0,0" />
        </StackPanel>
        <Button x:Name="ToggleButton" Content="{DynamicResource Start}" Style="{StaticResource PrimaryButton}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,6,0,0" MinWidth="108" AutomationProperties.Name="{DynamicResource ToggleAccessible}" />
      </Grid>
      <Grid Grid.Row="1" Margin="0,0,0,16">
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition />
          <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Border Style="{StaticResource Card}" Margin="0,0,12,0" Padding="20,15">
          <StackPanel>
            <TextBlock Text="{DynamicResource TotalUsage}" Foreground="{StaticResource Muted}" FontSize="11" />
            <TextBlock x:Name="TotalValue" Text="0.000 GB" FontSize="27" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,9,0,0" />
          </StackPanel>
        </Border>
        <Border Grid.Column="1" Style="{StaticResource Card}" Margin="0,0,12,0" Padding="20,15">
          <StackPanel>
            <TextBlock Text="{DynamicResource DownloadUsage}" Foreground="{StaticResource Muted}" FontSize="11" />
            <TextBlock x:Name="DownloadValue" Text="0.000 GB" Foreground="#5A63E8" FontSize="27" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,9,0,0" />
          </StackPanel>
        </Border>
        <Border Grid.Column="2" Style="{StaticResource Card}" Padding="20,15">
          <StackPanel>
            <TextBlock Text="{DynamicResource UploadUsage}" Foreground="{StaticResource Muted}" FontSize="11" />
            <TextBlock x:Name="UploadValue" Text="0.000 GB" Foreground="#20A7A0" FontSize="27" FontWeight="SemiBold" FontFamily="Segoe UI" Margin="0,9,0,0" />
          </StackPanel>
        </Border>
      </Grid>
      <Grid Grid.Row="2">
        <Border Background="#EDEFF6" CornerRadius="8" Padding="3" HorizontalAlignment="Left" VerticalAlignment="Top">
          <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="PeriodAll" GroupName="Period" Style="{StaticResource Segment}" Content="{DynamicResource AllTime}" IsChecked="True" Tag="All" />
            <RadioButton x:Name="PeriodToday" GroupName="Period" Style="{StaticResource Segment}" Content="{DynamicResource Today}" Tag="Today" />
            <RadioButton x:Name="PeriodMonth" GroupName="Period" Style="{StaticResource Segment}" Content="{DynamicResource Month}" Tag="Month" />
            <RadioButton x:Name="PeriodRange" GroupName="Period" Style="{StaticResource Segment}" Content="{DynamicResource CustomRange}" Tag="Range" />
          </StackPanel>
        </Border>
        <Button x:Name="ExportButton" Content="{DynamicResource Export}" HorizontalAlignment="Right" VerticalAlignment="Top" AutomationProperties.Name="{DynamicResource ExportAccessible}" />
      </Grid>
      <Border Grid.Row="3" Style="{StaticResource Card}" Padding="20,16,20,8">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="44" />
            <RowDefinition Height="*" />
            <RowDefinition Height="26" />
          </Grid.RowDefinitions>
          <Grid>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,5,0,0">
              <TextBlock Text="{DynamicResource NetworkUsage}" FontSize="14" FontWeight="SemiBold" />
              <TextBlock x:Name="NetworkCount" Text="{DynamicResource NoNetworks}" Foreground="#9AA5B8" FontSize="11" Margin="12,2,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
              <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                <Ellipse Width="6" Height="6" Fill="#6872EF" Margin="0,0,5,0" />
                <TextBlock Text="{DynamicResource Download}" FontSize="10" Foreground="#7C879D" />
                <Ellipse Width="6" Height="6" Fill="#54C4BE" Margin="12,0,5,0" />
                <TextBlock Text="{DynamicResource Upload}" FontSize="10" Foreground="#7C879D" />
              </StackPanel>
              <Border Background="#F0F2F8" CornerRadius="7" Padding="2" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                  <RadioButton x:Name="ChartView" GroupName="View" Style="{StaticResource Segment}" Content="{DynamicResource Chart}" Padding="12,0" Height="28" IsChecked="True" />
                  <RadioButton x:Name="TableView" GroupName="View" Style="{StaticResource Segment}" Content="{DynamicResource Details}" Padding="12,0" Height="28" />
                </StackPanel>
              </Border>
            </StackPanel>
          </Grid>
          <Grid Grid.Row="1">
            <ListBox x:Name="ChartList" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" AutomationProperties.Name="{DynamicResource ChartAccessible}">
              <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </ListBox.ItemsPanel>
              <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                  <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                  <Setter Property="Padding" Value="0" />
                  <Setter Property="Margin" Value="0,0,8,0" />
                  <Setter Property="Cursor" Value="Hand" />
                  <Setter Property="Template">
                    <Setter.Value>
                      <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="Transparent">
                          <ContentPresenter />
                        </Border>
                      </ControlTemplate>
                    </Setter.Value>
                  </Setter>
                </Style>
              </ListBox.ItemContainerStyle>
              <ListBox.ItemTemplate>
                <DataTemplate>
                  <Grid Height="73" ToolTip="{Binding SSID}">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="36" />
                      <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Border Width="25" Height="25" Background="#F0F2FA" CornerRadius="7" VerticalAlignment="Top" Margin="0,9,0,0">
                      <TextBlock Text="{Binding Rank}" Foreground="#8D97AD" FontFamily="Segoe UI" FontSize="10" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                    <Grid Grid.Column="1" Margin="7,7,0,8">
                      <Grid.RowDefinitions>
                        <RowDefinition Height="25" />
                        <RowDefinition Height="9" />
                        <RowDefinition />
                      </Grid.RowDefinitions>
                      <Grid>
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition />
                          <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" FontSize="12" FontWeight="SemiBold" Margin="0,0,12,0" />
                        <TextBlock Grid.Column="1" Text="{Binding TotalText}" FontFamily="Segoe UI" FontSize="12" Foreground="#65718A" />
                      </Grid>
                      <Border Grid.Row="1" Background="#F1F3F9" CornerRadius="4" ClipToBounds="True">
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="{Binding RxWidth}" />
                            <ColumnDefinition Width="{Binding TxWidth}" />
                            <ColumnDefinition Width="{Binding EmptyWidth}" />
                          </Grid.ColumnDefinitions>
                          <Border Background="#6872EF" CornerRadius="4,0,0,4" />
                          <Border Grid.Column="1" Background="#54C4BE" CornerRadius="0,4,4,0" />
                        </Grid>
                      </Border>
                      <TextBlock Grid.Row="2" Text="{Binding DetailText}" FontFamily="Segoe UI" Foreground="#A0A9BA" FontSize="10" Margin="0,5,0,0" />
                    </Grid>
                  </Grid>
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>
            <DataGrid x:Name="TrafficTable" Visibility="Collapsed" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False" CanUserReorderColumns="False" EnableRowVirtualization="True" EnableColumnVirtualization="True" HeadersVisibility="Column" GridLinesVisibility="None" RowHeaderWidth="0" BorderThickness="0" Background="White" AlternatingRowBackground="#FBFCFE" SelectionMode="Single" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" AutomationProperties.Name="{DynamicResource TableAccessible}">
              <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding DisplayName}" Width="*" />
                <DataGridTextColumn Binding="{Binding DownloadGB, StringFormat=N3}" Width="118" ElementStyle="{StaticResource NumericCell}" />
                <DataGridTextColumn Binding="{Binding UploadGB, StringFormat=N3}" Width="118" ElementStyle="{StaticResource NumericCell}" />
                <DataGridTextColumn Binding="{Binding TotalGB, StringFormat=N3}" Width="118" ElementStyle="{StaticResource NumericCell}" />
              </DataGrid.Columns>
            </DataGrid>
            <StackPanel x:Name="EmptyState" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,25">
              <TextBlock FontFamily="Segoe MDL2 Assets" Text="" FontSize="35" Foreground="#C7CCEC" HorizontalAlignment="Center" />
              <TextBlock Text="{DynamicResource EmptyTitle}" FontSize="16" Margin="0,16,0,8" HorizontalAlignment="Center" />
              <TextBlock Text="{DynamicResource EmptyDetail}" FontSize="12" Foreground="#929DB0" HorizontalAlignment="Center" />
            </StackPanel>
          </Grid>
          <Grid Grid.Row="2" Margin="0,7,0,0">
            <TextBlock x:Name="RangeCaption" Text="{DynamicResource DefaultCaption}" Foreground="#9AA5B8" FontSize="10" />
          </Grid>
        </Grid>
      </Border>
      <Grid Grid.Row="4" Margin="1,10,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="StatusDetail" Text="{DynamicResource BackgroundHint}" Foreground="#939EB1" FontSize="10" TextTrimming="CharacterEllipsis" VerticalAlignment="Top" />
        <Button x:Name="RefreshButton" Grid.Column="1" Content="{DynamicResource Refresh}" Height="22" Padding="8,0" FontSize="10" Background="Transparent" BorderThickness="0" Margin="10,-3,0,0" />
      </Grid>
    </Grid>
  </Grid>
</Window>
